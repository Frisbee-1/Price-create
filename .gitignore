# 📦 Папки, которые не нужны в Git
node_modules/
dist/

# ⚙️ Конфиги и временные файлы
.env
*.log
*.tmp

# 🔒 Данные проекта
data.json

# 💻 Системные файлы
.DS_Store
Thumbs.db

# Что это значит:
#node_modules/ — вся папка с зависимостями, она тяжёлая и всегда игнорируется.
#dist/ — собранный бандл Webpack, создаётся автоматически.
#.env, *.log — служебные файлы.
#data.json — твои данные из сервера (лучше не коммитить, чтобы не затирать).
#.DS_Store, Thumbs.db — мусор от Windows/macOS.